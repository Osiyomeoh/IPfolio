{
  "name": "ipfolio",
  "version": "1.0.0",
  "private": true,
  "description": "IPfolio - Index Funds for Intellectual Property on Story Protocol",
  "scripts": {
    "install": "cd frontend && npm install --legacy-peer-deps",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

